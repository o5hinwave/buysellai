# M11 First-Party iOS Redesign Audit

## Current Mandate Read - July 23, 2026

- Read `/Users/rhodes/.codex/attachments/ff163a97-7d64-4375-acb4-be7b3ead4838/pasted-text-1.txt` and its referenced `/Users/rhodes/.codex/attachments/2fd06f3b-40dc-49b0-9d8d-8b37a4ad8b26/pasted-text-1.txt` before continuing.
- The later Complete First-Party iOS Redesign Mandate overrides weaker earlier Sigma/custom-font guidance. The app should now use native SwiftUI and Apple platform patterns first, with restrained original product identity rather than a web-brand port.
- The addendum raises the listing standard: BuySell must feel like a calm native tool that quietly performs marketplace/listing expertise. User-facing UI must avoid search-marketing jargon, while the internal Listing Intelligence Engine can use Gemini, Google Search grounding, URL Context, deterministic fee/ranking rules, and cached official-source findings.

## Current Repo-Specific Audit - July 23, 2026

- Existing screens: Splash, Home, Camera, Snap Result, Marketplace Picker, Listing Draft, History rows, Auth, Email Sign-In, Settings, Delete Account, Tutorial, toasts, Safari legal pages, and app/release evidence docs.
- Existing navigation structure: one top-level Home `NavigationStack`, immersive full-screen Camera, full-screen Tutorial, system sheets for Auth/Settings, and native system sheets for Result, Marketplace, and Listing. The one-task information architecture is right, and the core flow now relies on SwiftUI sheet detents instead of a hand-built bottom-sheet shell.
- Existing visual inconsistencies: Home now has a first-viewport Apple Card-style centered camera card, restrained warm accent details, monochrome toolbar actions, Camera is immersive, and the downstream Result/Marketplace/Listing surfaces are native sheets. Remaining polish should focus on making the recommendation and listing moments feel less utilitarian under real device/VoiceOver review.
- Existing non-native or weaker components: several list sections used as product surfaces, plus prior historical audit text that contradicts current Supabase completion. The first-launch tutorial is now intentionally compact and visually tied to the Home direction through the same camera command mark and `Snap · Pick · Sell` rhythm.
- Existing usability problems: Home now opens with a calm camera mark, a `Snap · Pick · Sell` section, native task rows for snap/help, and saved listings as a quieter secondary section; Marketplace now opens with a clearer recommended answer before compare/all-place sections; Listing now leads with generated copy before marketplace, price, and tips context. Remaining usability proof depends on full-flow device and VoiceOver review.
- Existing accessibility risks: source guardrails cover many labels, Dynamic Type limits, Reduce Motion paths, and native flow-sheet detents, but real VoiceOver traversal and physical-device validation remain required before final acceptance.
- Existing missing states: Home now has empty history, pull-to-refresh, skeleton history loading, and a retryable history sync-failure row; Analyze, Marketplace, Listing, Camera, Auth, and Settings have useful loading/error/recovery states. Real-device camera, VoiceOver traversal, Instruments, latest-SDK, signed archive, App Store validation, and final acceptance evidence remain unproven.
- Existing camera weaknesses: the camera flow has AVFoundation preview, tap-to-focus/exposure, switching, torch gating, scene recovery, PhotosUI import, haptics, and permission recovery. It still needs physical-device proof for startup timing, memory, torch behavior, interruption recovery, and image quality.
- Existing listing intelligence status: the backend now asks Gemini for structured analyze facts with confidence, missing facts, and one simple photo prompt; asks Gemini for structured listing draft fields; uses Google Search plus URL Context when research cache is cold; saves grounding queries/sources to `marketplace_research_cache`; and formats `TITLE`/`DESCRIPTION` deterministically. The native app computes item-aware marketplace ranking, carries a bounded fit score on each item-aware estimate, stores internal fee source URLs and checked dates for marketplace playbooks, leads the picker with a plain recommended marketplace answer, and can surface one calm analyze hint before listing. Kidizen and Tradesy are preserved for history/API compatibility but excluded from active recommendations because current research shows they are retired as standalone marketplaces. Remaining addendum proof still depends on full-flow device/App Store validation rather than simulator-only checks.
- Proposed new navigation: preserve `Home -> Camera -> Result -> Marketplace -> Listing`, with Auth, Tutorial, Settings, History recovery, and Delete Account as secondary native tasks. Do not add tabs or dashboards.
- Proposed design system: keep semantic system typography, SF Symbols, semantic asset colors, native grouped surfaces where they genuinely fit, restrained functional accent color outside Home's first viewport, centralized motion/haptics, and Dynamic Type/Reduce Motion support. Avoid old custom font guidance, decorative gradients, nested cards, orange promotional panels, and web-dashboard icon grids.
- Proposed redesign sequence: refresh this audit, redesign Home into a first-party task command surface, then move Result/Marketplace/Listing from generic list sections toward calmer recommendation-focused sheet layouts, then expand Listing Intelligence data contracts, then validate camera/device/App Store gates.

## Existing Product Surface

- Home: launch/splash handoff, primary snap action, optional auth entry, settings entry, recent listings, empty history, pull-to-refresh.
- Camera: full-screen capture, permission recovery, camera-unavailable handling, flash, shutter feedback, offline/analyze recovery.
- Snap Result: analysis loading, success, inline name/price/category/condition editing, retry, retake, error recovery.
- Marketplace Picker: best/lowest summary, marketplace list, estimate badges, loading and fallback rows.
- Listing: generation loading, generated listing text, copy, regenerate, retake while keeping marketplace, success toast.
- History: local SwiftData history, remote Supabase history for signed-in users, swipe/delete confirmation, reopen listing.
- Auth: optional Sign in with Apple, email sign-in, guest escape, account/session persistence, local-to-remote migration.
- Tutorial: compact first-launch guide, keyboard dismissal support, Settings/Home re-entry, Reduce Motion-aware footer behavior.
- Settings: account state, theme and Reduce Motion preferences, tutorial, clear history, review prompt gate, legal/support links, delete account.

## Current Navigation Structure

- `AppRouter` owns a single state-driven primary flow with `NavigationStack` on Home, full-screen camera presentation, full-screen tutorial, system sheets for auth/settings, and native system sheets for result, marketplace, and listing.
- The top-level product intentionally avoids tabs because the product’s core model is one task: snap, pick, copy.
- Settings and auth are secondary tasks entered from Home and dismissed back to the primary flow.

## Redesign Findings

- The functional IA already matches the desired one-task product model; adding tabs or dashboards would make the app less first-party for this use case.
- Shared typography now follows San Francisco semantic Dynamic Type roles; remaining redesign work should continue replacing older custom visual compositions screen by screen.
- Home now uses a first-viewport Apple Card-like centered camera mark, restrained warm orange product signals, a simple `1 · 2 · 3` section rhythm, native snap/help rows, quiet saved-listings recovery section, and monochrome toolbar account/settings actions.
- Onboarding has moved from a long illustrated carousel to a compact native first-use guide that surfaces snap, pick, and copy steps immediately.
- Marketplace Picker has moved from a custom pinned material header to a native navigation/list sheet with inset grouped marketplace rows and native bordered best/lowest summary actions.
- Listing has moved from custom material panels to a native navigation/list compose sheet with toolbar dismissal and a system bottom action bar.
- Snap Result has moved from a custom scroll/pill composition to a native navigation/list review sheet with a sticky system decision action and bordered retake/retry/menu controls.
- Settings has moved to a quieter native grouped-list surface with standard row highlighting, disclosure indicators, toolbar icon buttons, and a native destructive account confirmation flow.
- Auth has moved from a centered custom pill setup screen to a native inset grouped sign-in sheet with row-based provider actions, `NavigationLink` email entry, toolbar dismissal, native form rows, and system bottom bars for guest/sign-in actions.
- Camera has moved toward a first-party capture surface with AVFoundation tap-to-focus/exposure, a focus indicator, capability-aware front/rear switching, scene-phase pause/restart recovery, torch gating, full-screen aspect-fill preview, and native PhotosUI import while preserving orientation handling and downsampled JPEG upload data.
- Native material and Liquid Glass fallbacks are centralized, but final SDK evidence remains pending until Xcode/iPhoneOS SDK 27+ is available.
- The camera and critical flow have strong simulator coverage, but final camera quality cannot be accepted until a trusted physical device and Instruments traces are recorded.
- Local loading, empty, offline, error, and success states are represented across tests and screens; production Supabase schema/functions/secrets are deployed and backend smoke evidence passes, while final signed App Store and physical-device evidence remain pending.
- App Store metadata and final acceptance docs deliberately preserve owner/legal placeholders until the account owner confirms them.

## Continuation Audit - July 22, 2026

- Existing visual inconsistencies: the app is mostly grouped-list and sheet based now. Older `brandFont` naming and unused custom pill button primitives have been removed; remaining capsule-shaped elements are limited to purposeful system-like chips, toasts, camera controls, and tutorial artwork details. Snap Result, Listing, Auth, Settings, Tutorial, and Camera recovery now rely on native bordered/prominent button treatment without explicit capsule shaping in their primary, recovery, destructive, and bottom-bar actions.
- Camera typography now uses direct SwiftUI semantic font roles for capture hints, startup copy, and recovery headers, keeping the capture surface closer to first-party iOS behavior instead of legacy brand-type helpers.
- Home camera mark, native task rows, footer copy, and empty-history copy now use direct SwiftUI semantic font roles, replacing the older promise-strip and orange-accent task hub while preserving native toolbar, refresh, swipe-delete, and history recovery behavior.
- Auth, Settings, and History rows now use direct SwiftUI semantic font roles as well, narrowing the remaining legacy typography surface to the result, marketplace, listing, and tutorial flows that need their own focused review.
- Listing draft, loading, error, tips, and bottom footer copy now use direct SwiftUI semantic font roles while preserving selectable generated text and native sticky actions.
- Marketplace top picks, rows, fallback copy, payout labels, and delta badges now use direct SwiftUI semantic font roles while preserving item-aware marketplace recommendation reasons and stable payout layout.
- Snap Result loading, inline edit, price, menu trigger, and error copy now use direct SwiftUI semantic font roles while preserving native list sections, menus, sticky decision action, and recovery order.
- Tutorial CTA, guide copy, and step labels now use direct SwiftUI semantic font roles in the compact first-use guide while preserving first-launch, re-entry, keyboard dismissal, and Reduce Motion-safe behavior.
- Existing non-native components: marketplace ranking originally reintroduced a stacked custom capsule summary inside an inset grouped list. That has been replaced with native list-row top picks so marketplace guidance feels like part of the iOS picker rather than a separate panel. Listing's sticky action bar has also been pruned back to a calmer native button hierarchy.
- Existing usability problems: marketplace choice now needs to explain not only payout, but search fit and listing quality. The picker now keeps the best, second, and third choices immediately tappable, with each row giving a concise reason for that item.
- Existing accessibility failures or risks: source tests cover Dynamic Type line limits, VoiceOver labels, chevrons, skeleton loading labels, and touchable row structure. Real VoiceOver and large-text traversal on physical devices is still required before final acceptance.
- Existing missing states: local skeleton, empty, fallback, offline, recoverable error, and success states are represented in code; production Supabase deploy and live backend smoke are now complete, while final signed-in/device acceptance still needs real App Store signing and physical-device validation.
- Existing camera weaknesses: simulator coverage cannot prove exposure quality, torch behavior, preview startup timing, interruption recovery, or capture memory behavior. Physical-device QA and Instruments traces remain mandatory.
- Proposed new navigation: preserve the single primary journey, `Home -> Camera -> Result -> Marketplace -> Listing`, with Auth, Tutorial, Settings, and History as secondary tasks entered through native sheets or navigation rows.
- Proposed design system: keep semantic colors, SF Dynamic Type roles, native grouped surfaces, system sheets, SF Symbols, restrained accent color, and Reduce Motion-aware feedback as the baseline. Avoid reintroducing nested cards, decorative glass, or capsule-heavy result/picker/listing/auth/settings/tutorial/camera-recovery layouts.
- Proposed redesign sequence: finish pruning custom pill/card patterns in the marketplace, listing, result, auth, and settings surfaces; then validate the camera on device; then close production Supabase, App Store metadata, archive, accessibility, and performance gates.
- Listing intelligence continuation: marketplace recommendations and listing generation now avoid exposing technical search-marketing acronyms in app/UI copy. The `generate-listing` edge function enables Gemini `googleSearch` and `urlContext` tools for marketplace-aware listing drafts, keeps official marketplace guidance ahead of stale assumptions, asks Gemini for structured draft fields instead of a precomposed listing blob, and then formats the copyable `TITLE`/`DESCRIPTION` contract deterministically before returning both `listing` and `draft` to the native app.
- Verification continuation: `Scripts/typecheck_local_sources.sh` and `Scripts/check_supabase_functions.sh` pass locally after the listing-intelligence changes. Direct `xcodebuild -list`, `xcodebuild build`, and focused `xcodebuild test` invocations in the live Documents folder stalled before build phases with idle `xcodebuild` processes in this environment. A clean `/tmp` snapshot build now succeeds with `xcodebuild -project /tmp/buysell-xcodebuild-snapshot.Yu8cpP/BuySellAI.xcodeproj -scheme BuySellAI -destination 'generic/platform=iOS Simulator' -configuration Debug CODE_SIGNING_ALLOWED=NO -derivedDataPath /tmp/BuySellAI-Snapshot-DerivedData -quiet build`.
- Search-planning continuation: Gemini now defaults to `gemini-2.5-flash`. Listing generation creates a deterministic minimal research plan of at most three marketplace questions, checks `marketplace_research_cache` first, skips live Search/URL-context tools when fresh saved research exists, and saves reusable findings/sources back through a service-role-only Supabase table with RLS forced and no client policies.
- Search-evidence continuation: `Scripts/check_supabase_functions.sh` now verifies the release function still gates Google Search and URL Context behind the saved cache and still saves Gemini grounding queries/sources back into `marketplace_research_cache`; the retained function log must include `listing research tools: google_search url_context gated-by-cache` and `listing research cache: Gemini grounding saved` before the combined M10 gate can pass.
- Verification continuation: `Scripts/check_supabase_schema.sh`, `Scripts/check_supabase_functions.sh`, and `Scripts/typecheck_local_sources.sh` pass after the research-cache migration, Gemini helper update, release-script marker updates, and backend source-test updates. Static source search shows no remaining `SEO` wording in app/backend sources.
- Supabase setup continuation: local app config is now written for project `czuoebqjajupghivqkch` using only the public publishable key in git-ignored `BuySellAI/App/Config.plist`. `supabase --version` reports `2.104.0`, `supabase init` confirms `supabase/config.toml` already exists, and `supabase start` is blocked until Docker Desktop is running because the CLI cannot connect to `/var/run/docker.sock`.
- Release account continuation: Xcode now uses Bundle ID `com.despia.buysellai` and Team ID `ZVFG6KC7KA`. Apple portal setup remains pending: enable Sign in with Apple on the App ID, create Services ID `com.despia.buysellai.signin` with callback `https://czuoebqjajupghivqkch.supabase.co/auth/v1/callback`, create the Apple `.p8` key, and configure Supabase Auth/server-side secrets without putting server keys in the app.
- Supabase SDK continuation: the app target now links the requested `Supabase` SwiftPM product from `https://github.com/supabase-community/supabase-swift`, pinned at `2.53.0` in Xcode's `Package.resolved`, with an `AppConfig.makeSupabaseClient()` factory available when the module resolves. Existing first-party HTTP clients remain the active contract path.
- Verification continuation: `/tmp` snapshot package resolution succeeds and pins `supabase-swift 2.53.0` plus transitive dependencies. A `/tmp` snapshot Debug simulator build succeeds with `xcodebuild ... -clonedSourcePackagesDirPath /tmp/BuySellAI-SourcePackages -quiet build`. Targeted tests for architecture guardrails, config security, backend function source contracts, Supabase preflight scripts, and signing capabilities pass. `Scripts/check_supabase_schema.sh`, escalated `Scripts/check_supabase_functions.sh`, `bash Scripts/typecheck_local_sources.sh`, and `Scripts/scan_m10_secrets.sh` pass after the Supabase SDK/config/signing changes.
- Support-site dependency continuation: the nested `AppStoreSite` project now pins `@supabase/server 1.4.1`, `next 16.2.11`, patched Vite/Cloudflare tooling, and npm overrides for `postcss 8.5.22`, `sharp 0.35.3`, and `esbuild 0.28.1`. `npm audit --json` reports zero vulnerabilities. The refreshed `npm run lint` evidence passes at `/tmp/buysell-support-site-lint.log`, and `npm test` passes the support, privacy, accessibility, terms, and home-page render checks at `/tmp/buysell-support-site-test.log`. Sites version 7 is now deployed publicly from support-site commit `14d6355e7a28484c513ecee29487602c18c21c6b`, and `/tmp/buysell-support-site-live.log` confirms the live home, support, privacy, accessibility, and terms routes return unauthenticated `200` HTML.
- Local evidence continuation: the expected readiness logs have been refreshed at `/tmp/buysell-local-source-typecheck.log`, `/tmp/buysell-submit-readiness-supabase-schema.log`, `/tmp/buysell-submit-readiness-supabase-functions.log`, and `/tmp/buysell-submit-readiness-secret-scan.log`. A clean `/tmp` snapshot Debug simulator build also passes with `CODE_SIGNING_ALLOWED=NO`, confirming the current iOS source still builds after the Supabase SDK and configuration work.
- Marketplace recommendation continuation: `MarketplaceEstimator.estimates(for:)` now keeps mainstream closet items such as a women's Madewell jacket on Poshmark instead of letting higher-fee-free payout alone push them to Vinted or Depop. Depop keeps a stronger score only when the item name signals vintage, streetwear, Y2K, or similarly trend-led resale. The focused item-aware estimator test and the full `MarketplaceEstimatorTests` suite pass, and the refreshed unit result bundle at `/tmp/buysell-full-unit-tests.xcresult` passes `477` `BuySellAITests`.
- Listing Intelligence scoring continuation: marketplace ranking now exposes named deterministic score components for sale likelihood, net payout, listing effort, shipping fit, local pickup fit, buyer trust, speed, and item fact quality. Bulky or fragile items get stronger local-pickup and shipping-friction handling without letting local marketplaces win specialist-fit items such as guitars, phones, closet fashion, or sneakers. The picker now leads with a plain `Best place to sell` answer row that says `We found the best place to sell this`, labels the first answer `Best chance to sell`, shows a compact `Take-home` amount, and moves alternatives into `Compare`; it still adds `Most money back` when the highest take-home estimate differs. Focused verification passes at `/tmp/buysell-marketplace-intelligence-focused-2.xcresult`, `/tmp/buysell-marketplace-summary-ui.xcresult`, `/tmp/buysell-marketplace-summary-design.xcresult`, and `/tmp/buysell-listing-intelligence-typecheck.log`.
- Listing research tool continuation: Google AI's Generate Content URL Context documentation was checked on July 23, 2026, and it shows REST tool payloads can combine `{ url_context: {} }` with `{ google_search: {} }`; the docs list Gemini 2.5 Flash as URL Context-capable and describe URL Context as useful for pulling specific findings from web pages. The `generate-listing` Edge Function now enables both tools only when the saved marketplace research cache is cold, then continues to save useful findings, grounding queries, and official sources back into `marketplace_research_cache`. `Scripts/check_supabase_functions.sh` and the combined M10 gate now require the retained marker `listing research tools: google_search url_context gated-by-cache`.
- Structured listing continuation: the result sheet can now show a calm `What we found` review with plain detected details and one `Could help` hint from structured analyze intelligence, marketplace rows now include plain fit cues backed by the deterministic item-aware score, and the listing sheet now puts the clean copyable `TITLE`/`DESCRIPTION` contract before the compact native `Price plan` rows for `List at`, `Likely sells for`, and `Take-home estimate`. The backend validates structured Gemini fields and emits `listing draft: structured fields formatted deterministically` as release evidence, while price, photo, fit, specifics, tags, and posting notes remain in structured native UI guidance instead of leaking into the clipboard.
- Marketplace evidence continuation: `Marketplace.playbookEvidence` now carries fee/rule source title, source URL, last-checked date, source kind, and active recommendation status for every marketplace. Active estimator and picker fallback lists use `Marketplace.activeRecommendationCases`, keeping Kidizen and Tradesy decodable for older rows while excluding them from new recommendation results.
- Archive evidence continuation: the no-sign Release archive verifier passes from `/tmp/buysell-submit-readiness-nosign-snapshot`, producing `/tmp/buysell-submit-readiness-nosign.xcarchive` and `/tmp/buysell-submit-readiness-nosign.log` with bundle ID `com.despia.buysellai`, release build `1.0 (1)`, App Store icon proof, no `UIDesignRequiresCompatibility`, no bundled custom fonts, no UI-test sample fixture markers in the archived executable, privacy manifest proof, and a `4800KB` app bundle.
- Google API continuation: a one-off `gemini-2.5-flash` request with the provided Google key returned HTTP `200` and `ok`. The key was not written to project files or retained evidence logs, and the refreshed secret scan still passes; rotate the pasted key before production.
- Supabase deploy continuation: Supabase CLI `2.104.0` is installed, the project is linked to `czuoebqjajupghivqkch`, required server-side secret names are present, Apple Auth is enabled for the app identifiers, and the refreshed backend smoke log at `/tmp/buysell-submit-readiness-backend.log` confirms the live schema and Edge Functions respond without adding server-side secrets to the app bundle or repo.
- Supabase MCP continuation: `codex mcp login supabase` has been re-authorized for the expanded scopes, and `codex mcp list` now shows the Supabase MCP server enabled with OAuth, project-scoped to `czuoebqjajupghivqkch`, and configured with the full requested feature set: docs, account, database, debugging, development, functions, branching, and storage.
- Supabase MCP/CLI refresh: Codex-side local approvals are set to `never`, the `sellai` project is trusted, global plugin permissions are set to the most permissive available mode (`review important actions`), and the Supabase MCP URL is no longer read-only. Live schema/function deployment still requires valid Supabase remote authorization, linked project state, and server-side secret names; the deploy and secret helpers now set `SUPABASE_TELEMETRY_DISABLED=1` by default after the CLI hit a local telemetry-file race during adjacent checks.
- Release blocker-log continuation: signed archive, App Store export, App Store validation, backend, Supabase deploy, metadata, real-device, acceptance, latest-design SDK, and Instruments blocker logs were refreshed under `/tmp/buysell-submit-readiness-*.log`. The signed archive and App Store export preflights now classify missing Apple account/provisioning-profile failures as structured pending evidence when `ALLOW_MISSING_TEAM=1` is set, preserving bundle ID `com.despia.buysellai`, Sign in with Apple entitlement proof, and release build `1.0 (1)` without pretending a signed archive exists.
- Simulator evidence continuation: a lean `/tmp` snapshot at `/tmp/buysell-m10-test-worktree.BQAjJl` refreshed the retained simulator artifacts. `/tmp/buysell-submit-readiness-focused.xcresult` passes `477` focused/unit tests, `/tmp/buysell-submit-readiness-full.xcresult` passes `509` unit/UI tests, `/tmp/buysell-m10-screenshots-iphone-16-pro-max-apple-card-20260723.xcresult` and `/tmp/buysell-m10-screenshots-ipad-pro-13-apple-card-20260723.xcresult` each pass the App Store screenshot capture test, `/tmp/buysell-submit-readiness-performance.log` passes the 5 simulator performance markers, and `/tmp/buysell-submit-readiness-ui.log` passes all `32` chunked UI evidence checks.
- Tutorial copy continuation: the first-launch tutorial is now a compact native guide with Skip, one `Start selling` action, three plain-language steps, keyboard dismissal, and no carousel pager. The marketplace step uses the calmer non-technical line “We suggest the best place first.”, matching the native simplicity mandate and keeping marketplace strategy out of onboarding. `Localizable.strings` lint, `git diff --check`, and `Scripts/typecheck_local_sources.sh` pass after the copy, UI-test assertion, and guardrail updates.
- Marketplace guidance copy continuation: Listing and picker guidance now uses plain first-party language for visible marketplace blurbs, recommendation reasons, and listing-sheet tips. The Listing sheet now labels the section `Quick tips` with `Main photo` and `What helps here`, and `MarketplaceEstimatorTests` verifies those visible guidance fields avoid search-marketing jargon such as SEO, keywords, buyers, boosting, ads, conversion, and ranking language. `Localizable.strings` lint, focused copy search, `git diff --check`, `Scripts/typecheck_local_sources.sh`, and focused Xcode `MarketplaceEstimatorTests` evidence now pass, with the retained result bundle at `/tmp/buysell-marketplace-copy-1784785601.xcresult`.
- Supabase deployment refresh: project `czuoebqjajupghivqkch` is linked locally, required server-side secret names are present, Supabase Auth Apple provider is enabled for `com.despia.buysellai.signin,com.despia.buysellai`, the remote schema and all four Edge Functions are deployed, and live backend smoke evidence passes at `/tmp/buysell-submit-readiness-backend.log` without writing provider or server-side secrets to the app bundle or repo.
- Shared typography primitive continuation: shared action, chip, and toast primitives now use direct SwiftUI semantic Dynamic Type roles (`headline` and `caption`) instead of the old `brandFont` helper. `BrandTextStyle`, the `brandFont` modifier, and unused `PrimaryPillButton`/`SecondaryPillButton`/`GhostButton` primitives have been deleted; the wordmark now maps directly to semantic system title roles with Bold Text-aware weights. `DesignAccessibilityTests`, `LocalizationCoverageTests`, and the latest-design verifier now guard against reintroducing those obsolete helpers.
- Obsolete primitive verification: `M10_ACCEPTANCE.md`, `README.md`, `ArchivePackagingScriptTests`, `M10SubmitReadinessScriptTests`, and the latest-design verifier now use the retained `obsolete button primitives:` and `active control glass:` markers instead of the older primary/standard button-glass wording. `rg` confirms the removed typography and pill-button helpers no longer appear in `BuySellAI` app sources, while negative source guardrails still watch for reintroduction.
- Native text-shaping continuation: visible listing and marketplace labels now use the native letter spacing and written capitalization from their semantic SwiftUI font roles instead of custom `.tracking(0.88)` or forced-uppercase overline treatment. `DesignAccessibilityTests` now scans app Swift sources to guard against reintroducing custom `.tracking`, `.kerning`, or `.textCase(.uppercase)` on visible UI text.
- Release fixture isolation continuation: UI-test sample photo and generated-listing fixtures are now compiled only under `#if DEBUG`; production paths cannot reference `ImageTools.sampleJPEG()` or `ListingFixtureText`. `Scripts/typecheck_local_sources.sh` now first emits a non-DEBUG `BuySellAIRelease` app module, then emits the DEBUG app module for unit/UI-test typechecking, and the no-sign archive verifier scans the archived Release executable before retaining `release fixtures: none bundled`. The combined readiness gate requires both the source `modes: release app debug app unit ui` marker and the no-sign archive `release fixtures: none bundled` marker.
- Combined readiness continuation: `ALLOW_PENDING_M10=1 bash Scripts/verify_m10_submit_readiness.sh M10_ACCEPTANCE.md` now runs with Supabase deploy/backend evidence present; remaining blockers are dirty source state, latest-design SDK 27+ evidence, Apple signing/export/validation, App Store owner/legal metadata, physical-device QA, Instruments traces, and final acceptance/result-log completion.
- Backend smoke refresh: `/tmp/buysell-submit-readiness-backend.log` now records live production schema/function/protected-resource markers plus analyze and listing smoke evidence for project `https://czuoebqjajupghivqkch.supabase.co`.
- Native sheet cleanup continuation: after Result, Marketplace, and Listing moved to SwiftUI system sheets, the unused `NativeMaterialSheetModifier` and `nativeMaterialSheet` helper were removed so the design system no longer carries hand-built bottom-sheet chrome.
- Marketplace icon continuation: marketplace rows now render SF Symbols chosen from the shared marketplace catalog, with restrained platform tint and no custom short-letter text marks.
- Home hierarchy continuation: the first screen now places the numbered `Take photo`, `Pick place`, `Copy listing` path above the primary `Snap to sell` action, reduces the empty saved-listings state, and renames history to `Saved listings` so it reads as recovery instead of a competing primary task.
- Marketplace recommendation continuation: the picker now uses a larger native recommendation row for the top answer, keeps `Most money back` and other alternatives in a `Compare` section, renames the full ranked list to `All places`, and reflows the recommendation row for accessibility text sizes.
- Listing copy continuation: the listing sheet title is now `Ready to copy` and the success state leads with the generated `TITLE`/`DESCRIPTION` text so the first viewport matches the app's copy-and-post promise. A native `Listing.RecommendationSummary` row still labels the selected marketplace as `Best chance to sell`, `Most money back`, or `Good place to sell`, shows the plain recommendation reason, and repeats the `Take-home` amount before the price plan.
- Tutorial hero continuation: the compact first-use guide now echoes the Home hero with the same centered `camera.viewfinder` command mark, supporting `sparkles`, `doc.on.clipboard`, `tag`, and `checkmark.circle` symbols, and the same `Snap · Pick · Sell` rhythm above the headline. The guide content is top-aligned inside the scroll area so the three steps fit cleanly above the single `Start selling` action on the iPhone 16 Pro simulator while preserving scroll fallback for larger text.
- Snap Result intelligence continuation: the review sheet now opens at the full-height native detent, preserves structured analyze details in `SnapResultStore`, and can show up to three plain `What we found` rows plus one `Could help` hint after the correction controls and before the user picks a marketplace, keeping confidence and missing-fact work available without exposing technical AI language.
- Snap Result Apple Card continuation: the post-photo review now opens as a pearl Apple Card-style confirmation surface with the user's photo, `We think this is`, calm category/condition fact pills, and a single orange `Yes, that's it` decision. `Not sure` now expands into plain possible-match guidance from `analysis.likelyMatches` or a better-photo cue, while `Change details` reveals the exact native edit controls only when needed. The `analyze-image` Edge Function contract now asks Gemini for up to three real possible matches when identity, model, or variant is uncertain, and the app sanitizes those into `Likely`, `Possible`, or `Maybe` labels instead of technical confidence percentages.
- Screenshot-flow continuation: the marketplace recommendation button now keeps the `MarketplaceSummary.bestChance.<marketplace>` automation identifier while using a concise VoiceOver summary, the visible recommendation sentence wraps instead of truncating, and the listing screen screenshot shows the generated listing text above supporting recommendation/price details. Focused verification passes at `/tmp/buysell-listing-marketplace-focused-20260723-1655.xcresult`, the quick iPhone 16 Pro screenshot flow passes at `/tmp/buysell-appstore-screenshots-20260723-1657.xcresult`, and the required App Store iPhone 6.9/iPad 13 screenshot bundles pass at `/tmp/buysell-m10-screenshots-iphone-16-pro-max-apple-card-20260723.xcresult` and `/tmp/buysell-m10-screenshots-ipad-pro-13-apple-card-20260723.xcresult`. The tracked PNG assets under `AppStoreAssets/Screenshots/iPhone-16-Pro-Max` and `AppStoreAssets/Screenshots/iPad-Pro-13-inch-M4` have been refreshed for the current Apple Card-style Home and `Ready to copy` flow, and `/tmp/buysell-submit-readiness-app-store-metadata.log` now passes screenshot dimensions, quality, brand-signal, and result-bundle checks with only account-owner legal/DSA metadata still pending.
- Marketplace tip copy continuation: visible listing-sheet fallback tips no longer use expert/marketing phrasing such as paid placement, traffic, price mechanics, or interest/watch-style language. The copy now gives calm one-step posting guidance such as `Post once the photos, details, and price feel clear.` and `Use this only when the shop, product page, and checkout are ready.` `MarketplaceEstimatorTests/testMarketplaceRecommendationReasonsAvoidTechnicalOptimizationLanguage` now guards against those terms returning in blurbs, recommendation focus, photo guidance, placement guidance, and per-item recommendation reasons. Focused verification passes at `/tmp/buysell-marketplace-copy-polish-2.xcresult`, and the snapshot source typecheck passes from `/tmp/buysell-copy-polish.oCTiBB`.
- Home Apple Card continuation: the Home first viewport now keeps the centered `camera.viewfinder` product mark inside a soft Apple Card-style surface, uses warm orange only for small brand/action signals, shows `1 · 2 · 3` as the beginner path, and hides the saved-listings section on a brand-new first run so the opening screen stays focused. `HomeView.swift`, `DesignAccessibilityTests`, `ArchivePackagingScriptTests`, and `Scripts/verify_m10_latest_design_sdk.sh` now guard those current Home markers. Refreshed evidence passes at `/tmp/buysell-local-source-typecheck.log`, `/tmp/buysell-submit-readiness-latest-design-sdk.log`, and `/tmp/buysell-submit-readiness-app-store-metadata.log`; simulator visual evidence is retained in the Apple Card screenshot bundles.
- Item questions continuation: the detail step now asks one calm, dynamic question at a time instead of showing a dense form. Questions are built from analyze missing facts, category needs, and saved answers before marketplace ranking, then marketplace selection routes through a short platform-specific follow-up before final listing generation. eBay, local marketplaces, fashion marketplaces, vintage/craft marketplaces, sneakers, tech, music gear, and cards each get distinct follow-up copy, with `I don't know`, Back, saved-answer review, and a no-extra-questions ready state. Source guardrails now assert the one-question flow, picker handoff, platform-specific question builder, saved-answer review, and removal of the old multi-field form. A source-only snapshot typecheck passes at `/tmp/buysell-item-questions-source.wIXIRU`.
- Item question memory continuation: the detail planner now carries analyze facts through Marketplace Picker into the marketplace-specific follow-up, asks the highest-impact missing photo fact before generic category prompts, skips questions when high-confidence photo facts already answer them, and remembers handled fields such as `I don't know` or `No` without sending those local memory markers as listing facts to the backend.
- Marketplace comparison continuation: the picker and listing recommendation summary now expose plain Apple-style decision labels, `Best overall`, `Fastest sale`, `Most money`, and `Easiest option`, backed by item-aware payout, speed, shipping, and local-pickup cues. Marketplace rows now add a simple `List around`, sale-speed, and fulfillment line so a non-expert can compare places without reading score mechanics, while VoiceOver receives the same comparison context.
- Listing evidence continuation: generated listings now keep the copyable post first, then place market checks, sold-price range, official fee source, checked date, public image search, and reference-image links behind one collapsed native `Evidence` disclosure. Reference images are explicitly labeled for checking the item only, not for posting, preserving the simple Apple Card-style flow while still exposing receipts when the user wants them.
- Structured source evidence continuation: the `generate-listing` Edge Function now asks Gemini for a bounded `evidenceSources` array and sanitizes each entry for marketplace/source title, URL, checked date, sold/active/official/reference status, condition or variant, comparability, and price. The iOS draft model deduplicates those sources, and the Listing sheet shows them only inside the collapsed `Evidence` disclosure so factual research support is available without turning the main flow into a research dashboard.
- Home pearl card continuation: the first screen now uses a soft off-white pearl background, a centered minimalist `camera.viewfinder` glyph inside a raised near-white squircle, and a restrained `1 2 3` badge so the front door reads like a calm Apple Card-style object. The three visible steps now say `Snap a photo`, `Answer what you know`, and `Copy the listing`, keeping the path obvious for a non-expert without adding instructions or extra marketplace jargon.
- Possible-match memory continuation: choosing a `Not sure` possible match now updates the detected item name and records `You picked` as the first structured analysis fact while removing that match from unresolved suggestions. The later question planner receives the adjusted analysis profile, so user confirmation is carried forward instead of being treated like another uncertain model guess.
- Listing field-copy continuation: the final listing sheet now keeps the primary `Copy listing` action, then exposes a calm native `Copy pieces` section for useful marketplace form fields: title, description, recommended price, details, and tags when the structured draft provides them. Copying one field writes only that field to the clipboard, gives success feedback, and keeps the user on the listing sheet.
- Apple Card polish continuation: the Home hero card now uses adaptive pearl ivory, mist, champagne, and pale peach tokens with a layered sheen, a raised centered `camera.viewfinder` glyph, and an in-card `Photo / Answer / Copy` strip. The native task rows remain below for scanning and accessibility, but the first visual object now carries the whole beginner flow without leaning on orange.
- Native icon continuation: Home and the first-use guide now use the same familiar iPhone-style symbols for the core loop: camera/viewfinder for taking the photo, message for answering, overlapping documents for copying, document text for saved listings, and info for the guide entry.
- Familiar icon vocabulary continuation: the app now routes the core selling-flow, category, condition, marketplace, copy, retry, retake, and compose symbols through `AppSymbol`, keeping the visible icon set close to common iPhone actions like camera, message, copy, cart, map pin, bag, phone, tag, and square-and-pencil. Guardrails now verify category and marketplace symbols stay inside that shared familiar set.
- Sold-comp evidence continuation: the `generate-listing` and `compare-marketplaces` Edge Functions now infer Sold versus Active status from structured evidence fields and only preserve comp low/median/high prices when sold or completed-price evidence is present. Active listings and asking prices can still support context, but they cannot populate sold-history price fields.
- Sold-price wording continuation: marketplace rows and listing evidence now use plain `sold prices` language instead of resale shorthand, including `No sold prices found`, `Sold prices X to Y`, and `Typical sold price X`. Marketplace row and summary VoiceOver labels now include the same grounded or limited sold-price cue that is visible on screen.

## Proposed System Direction

- Keep the one-task information architecture: Home to Camera to Result to Marketplace to Listing, with optional Auth, Tutorial, Settings, and History recovery.
- Move typography to native SF semantic roles (`largeTitle`, `title`, `title2`, `title3`, `body`, `caption`, `caption2`, `headline`) while preserving a small wordmark helper.
- Keep any warm accent as a restrained functional signal, not a first-viewport Home color layer.
- Continue using SF Symbols for functional iconography and reserve custom imagery for product identity moments such as the app icon or launch treatment.
- Use system sheets, semantic backgrounds, centralized material surfaces, and Reduce Motion-aware transitions.

## Redesign Sequence

1. Migrate shared typography away from bundled static font faces and into native Dynamic Type system roles.
2. Keep Home's legacy hero/pill task hub replaced by the native grouped camera-card surface, native task rows, and toolbar affordances.
3. Keep launch and SwiftUI splash visually aligned through system-bold wordmark styling and shared launch color tokens.
4. Replace the legacy onboarding carousel with a compact first-use guide that preserves first-launch and re-entry behavior.
5. Validate the redesigned Camera surface on a trusted physical device, including preview startup, focus/exposure behavior, switching, torch/flash behavior, capture freeze, and processing handoff.
6. Preserve the current native material layer and continue final SDK verification when the latest toolchain is available.
7. Use real Supabase config/deploy evidence, signed archive/export evidence, physical-device acceptance, and Instruments traces to close the remaining M10 gates.
